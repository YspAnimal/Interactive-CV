---
title: "Interactive CV"
output: 
  flexdashboard::flex_dashboard:
    #social: menu
    source_code: https://github.com/YspAnimal/Interactive-CV
    css: bootstrap.css
    #- { title: "Source code", href: "https://github.com/YspAnimal/Interactive-CV", icon: "github" , align: right }

runtime: shiny
---

```{r setup, include=FALSE}
library(flexdashboard)
library(data.table)
library(ggplot2)
library(dplyr)
library(purrr)
library(timevis)
library(lubridate)
library(openxlsx)
timeLineData <- read.xlsx("./WorkingPath.xlsx", sheet = 1, detectDates = TRUE)
timeLineData$content <- timeLineData$place
projects <- read.xlsx("./WorkingPath.xlsx", sheet = 2)
skills <- read.xlsx("./WorkingPath.xlsx", sheet = 3)



```

Column {.sidebar}
======================
<br>
<style>
img {
  border-radius: 50%;
}
</style>
<img src="myphoto.jpeg" alt="Avatar" style="width:200px">

**NIKOLAY SOLOVEY**

IT specialist

Male, 32 year`s old

Bachelor degree in Mathematics

Specialist degree in Applied Math

About
======================
About





Working Path
======================
### Working Path
```{r}
timevisOutput('timeLine')
output$timeLine <- renderTimevis({
  timevis(timeLineData, showZoom = FALSE, height = 250)
  })
tags$div(id="skillsButtons")

### This is working on single shiny app, but doesnt work with flexdashboard. I will try to write a module or external app for it
observeEvent(input$timeLine_selected, {
  selectedOrg <- timeLineData %>% 
                    filter(id == as.numeric(input$timeLine_selected)) %>% 
                    select(place)
  selectedSkills <- skills %>% 
                      filter(company %in% selectedOrg)
  idNum <- 0
  skillsButtons <- map(selectedSkills$keySkills, function(x){
    print(x)
    idNum <<- idNum + 1
    id <- paste0('skill', idNum)
    actionButton(id,x)
  })
  print(str(skillsButtons))
  fluidRow(
    insertUI(
      selector = "#skillsButtons", 
      ui = skillsButtons
    )
  )
})



```







Learning Path
======================
Learning Path

